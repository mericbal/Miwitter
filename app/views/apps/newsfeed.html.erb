<h2> your newsfeed </h2>

<h2> <%= current_user.following_users.count %> </h2>

<% current_user.following_users.each do |u|  %>
		<h2> from, <%= u.username %> <%= link_to '->', user_path(u) %> </h2>
	<% u.tweets.order(created_at: :desc).each do |t| %>
		<% if (Time.now - t.created_at) <= 5000 %>
			<li id="green"> <%= t.body %> <span id="red"> <%= t.posted_when? %> </span> </li>
		<% else %>
			<li id="green"> <%= t.body %> </li>
		<% end %>
	<% end %>
<% end %>